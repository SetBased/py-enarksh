#!/usr/bin/python3

import os
import sys

sys.path.insert(0, os.path.realpath(os.path.dirname(os.path.realpath(__file__)) + '/..'))

from enarksh.application.EnarkshApplication import EnarkshApplication

enarksh = EnarkshApplication()
enarksh.run()
