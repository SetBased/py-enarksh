#!/usr/bin/python3
import os
import sys

sys.path.append(os.path.realpath(os.path.dirname(os.path.realpath(__file__)) + '/..'))

from lib.enarksh.Util.LoadSchedule import LoadSchedule

reader = LoadSchedule()
reader.main()
