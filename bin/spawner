#!/usr/bin/python3

import os
import sys

sys.path.insert(0, os.path.realpath(os.path.join(os.path.dirname(__file__), '..')))

from enarksh.application.SpawnerApplication import SpawnerApplication

spawner = SpawnerApplication()
status = spawner.run()

exit(status)
