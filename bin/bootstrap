#!/usr/bin/python3
import os
import sys

sys.path.append(os.path.realpath(os.path.dirname(os.path.realpath(__file__)) + '/..'))

from lib import enarksh
from lib.enarksh.Bootsrap import Bootstrap

os.chdir(enarksh.HOME)

bootstrap = Bootstrap()
ret = bootstrap.main()

exit(ret)